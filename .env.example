# Postgres lyai_db (Docker container lyai_postgres)
DATABASE_URL=postgres://lyai:lyai@localhost:5432/lyai_db

# Anthropic API (extracción de claims + LLM-juez de contradicciones)
ANTHROPIC_API_KEY=sk-ant-...

# Voyage AI (embeddings de claims para búsqueda de candidatos, ver
# pipeline/embed_claims.py)
VOYAGE_API_KEY=pa-...

# CORS de api/main.py: orígenes separados por coma. Normalmente NO hace
# falta en dev — vite.config.ts ya proxea /api/* a uvicorn bajo el mismo
# origen (localhost:3000), igual que Traefik hace en producción. Solo
# rellenar si algo pega directo a la API desde otro origen (puerto 8000).
API_CORS_ORIGINS=
